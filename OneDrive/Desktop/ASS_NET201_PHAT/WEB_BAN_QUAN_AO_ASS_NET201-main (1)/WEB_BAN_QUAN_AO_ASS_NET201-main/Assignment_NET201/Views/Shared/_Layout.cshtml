<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ICONDENIM</title>
    <link href="https://fonts.googleapis.com/css2?family=Anton&family=Inter:wght@300;400;500;600&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        /* Search Overlay Styles */
        #searchOverlay {
            position: fixed;
            top: -100%;
            left: 0;
            width: 100%;
            height: auto;
            background: white;
            z-index: 9999;
            transition: top 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            padding: 2rem 0;
        }

        #searchOverlay.active {
            top: 0;
        }

        .search-backdrop {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 9998;
            display: none;
            backdrop-filter: blur(3px);
        }

        .search-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .search-input-group {
            position: relative;
            max-width: 600px;
            margin: 0 auto;
        }

        .search-input-group input {
            width: 100%;
            padding: 12px 50px 12px 20px;
            border: 1px solid #000;
            border-radius: 4px;
            font-size: 1rem;
            outline: none;
        }

        .search-input-group .btn-search {
            position: absolute;
            right: 5px;
            top: 50%;
            transform: translateY(-50%);
            background: none;
            border: none;
            font-size: 1.2rem;
        }

        .popular-keywords {
            margin-top: 1.5rem;
            text-align: center;
        }

        .keyword-tags {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 10px;
            margin-top: 10px;
        }

        .keyword-tags a {
            padding: 5px 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
            color: #333;
            text-decoration: none;
            font-size: 0.85rem;
            transition: all 0.2s;
        }

        .keyword-tags a:hover {
            border-color: #000;
            background: #f8f9fa;
        }

        .close-search {
            position: absolute;
            right: 20px;
            top: 20px;
            cursor: pointer;
            font-size: 1.5rem;
            color: #888;
        }
    </style>
</head>

<body>
    <!-- Search Overlay -->
    <div class="search-backdrop" id="searchBackdrop"></div>
    <div id="searchOverlay">
        <span class="close-search" id="closeSearch"><i class="fas fa-times"></i></span>
        <div class="search-container">
            <div class="row align-items-center">
                <!-- Left: Logo -->
                <div class="col-md-3 d-none d-md-block">
                    <a class="fw-bold text-uppercase fs-4 text-dark text-decoration-none" style="font-family: 'Anton', sans-serif;" href="/">ICONDENIM</a>
                </div>
                
                <!-- Center: Search Box -->
                <div class="col-md-6 col-12">
                    <form action="/Home/Shop" method="get">
                        <div class="search-input-group">
                            <input type="text" name="searchString" placeholder="Tìm kiếm sản phẩm..." autocomplete="off">
                            <button type="submit" class="btn-search"><i class="fas fa-search"></i></button>
                        </div>
                    </form>
                    <div class="popular-keywords">
                        <div class="fw-bold small text-uppercase" style="letter-spacing: 1px;">Từ khóa nổi bật hôm nay</div>
                        <div class="keyword-tags">
                            <a href="/Home/Shop?searchString=smartjean">smartjean</a>
                            <a href="/Home/Shop?searchString=Ao thun">Áo thun</a>
                            <a href="/Home/Shop?searchString=Ao polo">Áo polo</a>
                            <a href="/Home/Shop?searchString=Quan short">Quần short</a>
                            <a href="/Home/Shop?searchString=Ao khoac">Áo khoác</a>
                            <a href="/Home/Shop?searchString=Quan tay">Quần tây</a>
                        </div>
                    </div>
                </div>

                <!-- Right: Icons -->
                <div class="col-md-3 d-none d-md-flex justify-content-end gap-3 align-items-center">
                    <a href="/Account/Login" class="text-dark fs-5"><i class="far fa-user"></i></a>
                    <a href="/Cart/Wishlist" class="text-dark fs-5"><i class="far fa-heart"></i></a>
                    <a href="/Cart" class="text-dark fs-5"><i class="fas fa-shopping-bag"></i></a>
                </div>
            </div>
        </div>
    </div>
    <header class="sticky-top bg-white border-bottom">
        <nav class="navbar navbar-expand-lg navbar-light py-3">
            <div class="container">
                <!-- Brand -->
                <a class="navbar-brand fw-bold text-uppercase fs-3" asp-controller="Home" asp-action="Index"
                    style="font-family: 'Anton', sans-serif; letter-spacing: 1px;">
                    ICONDENIM
                </a>

                <!-- Toggler -->
                <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarContent">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navigation -->
                <div class="collapse navbar-collapse" id="navbarContent">
                    <ul class="navbar-nav mx-auto mb-2 mb-lg-0 fw-medium">
                        <li class="nav-item">
                            <a class="nav-link text-uppercase px-3" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-uppercase px-3" asp-controller="Home" asp-action="Shop">Shop</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-uppercase px-3" asp-controller="Home" asp-action="Shop"
                                asp-route-categoryId="1">T-Shirts</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-uppercase px-3" asp-controller="Home" asp-action="Shop"
                                asp-route-categoryId="2">Jeans</a>
                        </li>
                    </ul>

                    <!-- Icons -->
                    <div class="d-flex align-items-center gap-3">
                        <a href="javascript:void(0)" class="text-dark fs-5" id="openSearch"><i class="fas fa-search"></i></a>
                        
                        @using Microsoft.AspNetCore.Identity
                        @using Assignment_NET201.Models
                        @inject SignInManager<AppUser> SignInManager
                        @inject UserManager<AppUser> UserManager

                        @if (SignInManager.IsSignedIn(User))
                        {
                            <div class="dropdown">
                                <a href="#" class="text-dark fs-5 dropdown-toggle" data-bs-toggle="dropdown">
                                    <i class="far fa-user"></i>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end rounded-0 shadow-sm border-0">
                                    @{
                                        var currentUser = await UserManager.GetUserAsync(User);
                                    }
                                    <li><span class="dropdown-item-text text-muted small">Hello, @(currentUser?.FullName ?? User.Identity?.Name)</span></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" asp-controller="Account" asp-action="Profile">My Profile</a></li>
                                    <li><a class="dropdown-item" asp-controller="Account" asp-action="MyOrders">My Orders</a></li>
                                    @if(User.IsInRole("Admin"))
                                    {
                                         <li><a class="dropdown-item fw-bold" asp-controller="Admin" asp-action="Index">Admin Dashboard</a></li>
                                    }
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <form class="form-inline" asp-controller="Account" asp-action="Logout" method="post">
                                            <button type="submit" class="dropdown-item text-danger">Logout</button>
                                        </form>
                                    </li>
                                </ul>
                            </div>
                        }
                        else
                        {
                             <a asp-controller="Account" asp-action="Login" class="text-dark fs-5"><i class="far fa-user"></i></a>
                        }

                        @using Assignment_NET201.Extensions
                        @using Assignment_NET201.ViewModels
                        @{
                            var cartCount = Context.Session.Get<List<CartItem>>("Cart")?.Count ?? 0;
                            var wishlistCount = Context.Session.Get<List<WishlistItem>>("Wishlist")?.Count ?? 0;
                        }

                        <a asp-controller="Cart" asp-action="Wishlist" class="text-dark fs-5 position-relative me-2">
                            <i class="far fa-heart"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size: 0.6rem;">@wishlistCount</span>
                        </a>

                        <a asp-controller="Cart" asp-action="Index" class="text-dark fs-5 position-relative">
                            <i class="fas fa-shopping-bag"></i>
                            <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-black" style="font-size: 0.6rem;">@cartCount</span>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <footer class="text-white pt-5 pb-3 mt-5" style="background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 50%, #1a1a1a 100%);">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-4">
                    <h5 class="text-uppercase fw-bold mb-3" style="font-family: 'Anton', sans-serif;">ICONDENIM</h5>
                    <p class="text-secondary small">Reimagining modern streetwear for the bold and ambitious.</p>
                    <div class="d-flex gap-3 mt-3">
                        <a href="#" class="text-white fs-5"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-white fs-5"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-white fs-5"><i class="fab fa-tiktok"></i></a>
                    </div>
                </div>
                <div class="col-md-4">
                    <h6 class="text-uppercase fw-bold mb-3">Quick Links</h6>
                    <ul class="list-unstyled small text-secondary">
                        <li class="mb-2"><a href="#" class="text-decoration-none text-secondary hover-white">About Us</a></li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-secondary">Shipping & Returns</a>
                        </li>
                        <li class="mb-2"><a href="#" class="text-decoration-none text-secondary">FAQ</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h6 class="text-uppercase fw-bold mb-3">Newsletter</h6>
                    <p class="text-secondary small mb-3">Subscribe to get exclusive offers and updates.</p>
                    <div class="input-group mb-3">
                        <input type="text" class="form-control border-0 text-white" style="background: rgba(255,255,255,0.1);"
                            placeholder="Enter your email">
                        <button class="btn text-dark fw-bold" style="background: linear-gradient(to right, #fff, #ddd);" type="button">Subscribe</button>
                    </div>
                </div>
            </div>
            <hr class="border-secondary my-4">
            <div class="text-center text-secondary small">
                &copy; 2026 ICONDENIM. All rights reserved.
            </div>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <script>
        $(document).ready(function() {
            const $overlay = $('#searchOverlay');
            const $backdrop = $('#searchBackdrop');
            const $openBtn = $('#openSearch');
            const $closeBtn = $('#closeSearch');

            $openBtn.on('click', function() {
                $overlay.addClass('active');
                $backdrop.fadeIn(300);
                setTimeout(() => $overlay.find('input').focus(), 400);
            });

            function closeSearch() {
                $overlay.removeClass('active');
                $backdrop.fadeOut(300);
            }

            $closeBtn.on('click', closeSearch);
            $backdrop.on('click', closeSearch);

            // Close on ESC key
            $(document).on('keydown', function(e) {
                if (e.key === "Escape") closeSearch();
            });
        });
    </script>
    @await RenderSectionAsync("Scripts", required: false)
</body>

</html>
